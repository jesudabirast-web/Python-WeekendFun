#prompt user to input numbers(integers)
#sum up the integers inputed from the user
#calculate the average and input of user's numbers 
#for loop because the scripts to have a loop that inputs four integers



number_one = int(input("Enter the first integer: "))
number_two = int(input("Enter the second integer: "))
number_three = int(input("Enter the third integer: "))
number_four = int(input("Enter a fourth integer: "))

sum = number_one + number_two + number_three + number_four
average = sum / 4
product = number_one * number_two * number_three * number_four

count < 4
for 

